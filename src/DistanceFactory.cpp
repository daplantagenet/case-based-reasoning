#include "DistanceFactory.h"
#include "Utility.h"
#include "DistanceRF.h"

double DistanceFactory::calc_distance(const arma::rowvec& X, const arma::rowvec& Y) {
  return this->calc_distance(X, Y);
}